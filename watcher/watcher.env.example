# Scanarr Watcher Configuration (V1.5 — Phase 5)
# Copy this file to /etc/scanarr/watcher.env and adjust values
#
# Only 2 variables are needed. All other configuration (watch paths,
# log level, scan on start, reconnect delay, etc.) is delivered by
# the API server after authentication via WebSocket.

# WebSocket URL of the Scanarr API server
SCANARR_WS_URL=ws://localhost:8081/ws/watcher

# Unique identifier for this watcher instance (shown in the UI)
# Must be unique across all watcher instances in your setup
SCANARR_WATCHER_ID=my-server-watcher

# Optional: override the state file path (default: /etc/scanarr/watcher-state.json)
# SCANARR_STATE_PATH=/etc/scanarr/watcher-state.json
