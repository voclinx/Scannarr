# === DATABASE ===
DB_PASSWORD=scanarr_secret

# === SYMFONY ===
APP_ENV=prod
APP_SECRET=change_me_in_production_with_random_string
CORS_ALLOW_ORIGIN='^https?://(localhost|scanarr\.local)(:[0-9]+)?$'

# === JWT ===
JWT_PASSPHRASE=scanarr_jwt
JWT_TOKEN_TTL=3600

# === WATCHER AUTH (shared between Docker API and native watcher) ===
WATCHER_AUTH_TOKEN=secret-watcher-token

# === TMDB ===
TMDB_API_KEY=your_tmdb_api_key_here

# === PORTS ===
# FRONT_PORT=3000
# FRONT_CONTAINER_PORT=80          # Production (nginx)
# FRONT_CONTAINER_PORT=5173        # Development (Vite)

# === VOLUMES (host paths mounted into the API container for file deletion) ===
MEDIA_VOLUME_1=/mnt/media1
MEDIA_VOLUME_2=/mnt/media2
